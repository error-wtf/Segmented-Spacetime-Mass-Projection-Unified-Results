# SSZ Theory - Google Colab Compatible Requirements
# © 2025 Carmen Wrede & Lino Casu
# Version: 2.0 Final - Extended
#
# Use this in Colab instead of requirements.txt
#
# Colab pre-installs: numpy, scipy, matplotlib, pandas, requests
# We only install packages NOT pre-installed in Colab

# ============================================================================
# ASTRONOMY & ASTROPHYSICS
# ============================================================================
astropy>=5.3.0
astroquery>=0.4.6

# ============================================================================
# INTERACTIVE VISUALIZATION
# ============================================================================
plotly>=5.14.0
dash>=2.14.0
seaborn>=0.12.0  # Statistical visualization (not pre-installed)

# ============================================================================
# DATA FORMATS & I/O
# ============================================================================
pyarrow>=12.0.0
h5py>=3.8.0

# ============================================================================
# IMAGE & ANIMATION
# ============================================================================
pillow>=10.0.0  # Usually pre-installed but may need upgrade
imageio>=2.31.0  # For GIF/video generation
imageio-ffmpeg>=0.4.0  # FFmpeg wrapper

# ============================================================================
# PERFORMANCE
# ============================================================================
numba>=0.57.0  # JIT compilation (not pre-installed in Colab)

# ============================================================================
# UTILITIES
# ============================================================================
tqdm>=4.65.0  # Usually pre-installed but may need upgrade
PyYAML>=6.0  # Config file parsing
jsonschema>=4.0.0  # JSON validation
tabulate>=0.9.0  # Pretty tables
colorama>=0.4.6  # Colored output

# ============================================================================
# TESTING
# ============================================================================
pytest>=7.4.0
pytest-timeout>=2.1.0
pytest-cov>=4.0.0  # Code coverage

# ============================================================================
# IMPORTANT NOTES FOR COLAB
# ============================================================================

# The following are pre-installed in Colab and should NOT be reinstalled:
# - numpy (1.25.x)
# - scipy (1.11.x)
# - matplotlib (3.7.x)
# - pandas (2.0.x)
# - requests (2.31.0) ← Pinned by google-colab, do not upgrade!

# Installation in Colab:
# !pip install -r requirements-colab.txt

# Or install selectively (recommended for faster setup):
# !pip install astropy astroquery plotly seaborn imageio numba PyYAML

# ============================================================================
# PACKAGE COUNT: 18 packages (excluding Colab pre-installed)
# ============================================================================
